replicaCount: 1

image:
  repository: ghcr.io/busterswt/draino-claude
  tag: main

gateway:
  enabled: true
  create: false
  gatewayClassName: envoy-gateway-class
  name: shared-gateway
  parentRefs:
    - name: shared-gateway
      namespace: ""
      sectionName: https
  hostnames:
    - draino.example.com

nodeAgent:
  enabled: true

ssh:
  enabled: false
  secretName: draino-ssh

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi
